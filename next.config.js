/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: true,

    images: {
        domains: ['makarska-info.hr', 'i.ytimg.com', 'countryflagsapi.com'],
    },
}

module.exports = nextConfig
